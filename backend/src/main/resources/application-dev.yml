spring:
  config:
    activate:
      on-profile: dev

  devtools:
    add-properties: true

  datasource:
    url: jdbc:mysql://${DB_HOST:}:${DB_PORT:3306}/${DB_NAME:social_network}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USER:}
    password: ${DB_PASS:}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  sendgrid:
    api-key: ${SENDGRID_API_KEY}
    from-email: "manhphongg6572@gmail.com"

jwt:

  key2: "8mhZcWZVPhDeucdGaUQhUQoSkda3+jAtkBPJNP/4dvQ="

admin: { email_default: "manhphong6572@gmail.com", password_default: "12345678" }

